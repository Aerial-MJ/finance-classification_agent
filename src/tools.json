{
    "tools":[
        {
            "type": "function",
            "function": {
            "name": "invoke_VLM_model",
            "description": "Analyzes an image using a vision-language model (Qwen2.5-VL) to extract key fields, layout features, and classify the file type",
            "parameters": {
                "type": "object",
                "properties": {
                "image_rotate_path": {
                    "type": "string",
                    "description": "Path to the rotated image file"
                }
                },
                "required": ["image_rotate_path"]
            }
            }
        },
        {
            "type": "function",
            "function": {
            "name": "invoke_ocr_layoutLMv3_model",
            "description": "Performs OCR and named entity recognition (NER) on an image using LayoutLMv3 to extract and annotate structured information",
            "parameters": {
                "type": "object",
                "properties": {
                "image_rotate_path": {
                    "type": "string",
                    "description": "Path to the rotated image file"
                }
                },
                "required": ["image_rotate_path"]
            }
            }
        },
        {
            "type": "function",
            "function": {
            "name": "invoke_classification_model",
            "description": "Classifies an image-text pair using a fine-grained ResNet and BERT fusion model to predict document categories",
            "parameters": {
                "type": "object",
                "properties": {
                "image_rotate_path": {
                    "type": "string",
                    "description": "Path to the rotated image file"
                },
                "ocr_text": {
                    "type": "string",
                    "description": "Text extracted from OCR"
                }
                },
                "required": ["image_rotate_path", "ocr_text"]
            }
            }
        },
        {
            "type": "function",
            "function": {
            "name": "invoke_rag_model",
            "description": "Initializes a retrieval-augmented generation (RAG) system with tools for querying a knowledge base",
            "parameters": {
                "type": "object",
                "properties": {},
                "required": []
            }
            }
        },
        {
            "type": "function",
            "function": {
            "name": "invoke_deepseek_model",
            "description": "Summarizes input text and performs classification using the DeepSeek model",
            "parameters": {
                "type": "object",
                "properties": {
                "text": {
                    "type": "string",
                    "description": "Input text for summarization and classification"
                }
                },
                "required": ["text"]
            }
            }
        }
    ]
}